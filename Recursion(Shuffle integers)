void shuffleArray(int arr[],int n)
	{
	    // Your code goes here
	     vector<int>out;
        for(int x=0; x < n/2; x++){      //05-feb-2023
            out.push_back(arr[x]);
            out.push_back(arr[n/2+x]);
        }
        for(int i = 0; i < n; i++)
            arr[i] = out[i];
	}
